//Write a function that prints out the numbers from 1 to 10.
//Use while loop

function printNumbers() {
    let currentNum = 0;
    
    while(currentNum < 10) {
        currentNum = currentNum + 1;
        console.log(currentNum)
    }
}

printNumbers(["10"])